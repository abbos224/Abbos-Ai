// Your computer's LAN IP where the backend server is running (npm run dev in /server).
// Your phone must be on the SAME Wi-Fi network as this computer.
// If uploads fail with a network error, run `ipconfig` on the computer and update this IP
// (look for the IPv4 address of your active Wi-Fi adapter — NOT a VPN adapter like NordLynx/Tailscale).
export const API_BASE_URL = 'http://10.39.1.28:4000';
